credentials:
  system:
    domainCredentials:
      - credentials:
          - gitHubApp:
              id: "github-app-123"
              appID: "123"  #"${GITHUB_APP_ID}"
              privateKey: "456" #"${GITHUB_APP_PRIVATE_KEY}"
              repositoryAccessStrategy:
                specificRepositories: {}
          - gitHubApp:
              id: "github-app-78"
              appID: "90"  #"${GITHUB_APP_ID}"
              privateKey: "456" #"${GITHUB_APP_PRIVATE_KEY}"
              repositoryAccessStrategy:
                specificRepositories: {}