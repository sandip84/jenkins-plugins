credentials:
  system:
    domainCredentials:
      - credentials:
          - gitHubApp:
              id: "github-app"
              appID: "${GITHUB_APP_ID}"
              privateKey: "${GITHUB_APP_PRIVATE_KEY}"
              repositoryAccessStrategy:
                specificRepositories: {}